import { View, Text } from 'react-native';
import React from 'react';

const MovieReviewListingScreen = () => {
    return (
        <View>
            <Text>MovieReviewScreen</Text>
        </View>
    );
};

export default MovieReviewListingScreen;